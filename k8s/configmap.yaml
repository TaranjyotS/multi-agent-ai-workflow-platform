apiVersion: v1
kind: ConfigMap
metadata:
  name: multi-agent-config
  namespace: multi-agent-ai
data:
  ENVIRONMENT: production
  ENABLE_API_KEY_AUTH: "true"
  QDRANT_COLLECTION: enterprise_knowledge
